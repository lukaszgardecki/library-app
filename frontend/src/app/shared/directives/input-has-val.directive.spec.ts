import { InputHasValDirective } from './input-has-val.directive';

describe('InputHasValDirective', () => {
  it('should create an instance', () => {
    const directive = new InputHasValDirective();
    expect(directive).toBeTruthy();
  });
});
