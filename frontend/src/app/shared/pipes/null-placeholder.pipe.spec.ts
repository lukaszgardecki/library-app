import { NullPlaceholderPipe } from './null-placeholder.pipe';

describe('NullPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new NullPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
