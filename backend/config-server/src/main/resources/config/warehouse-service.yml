server:
  port: 8089
spring:
  liquibase:
    change-log: classpath:db/changelog/master.xml
  kafka:
    consumer:
      properties:
        spring:
          json:
            trusted:
              packages: com.example.warehouseservice.domain.event
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka